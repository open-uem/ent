// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/open-uem/ent/mdmcommand"
	"github.com/open-uem/ent/predicate"
)

// MDMCommandQuery is the builder for querying MDMCommand entities.
type MDMCommandQuery struct {
	config
	ctx        *QueryContext
	order      []mdmcommand.OrderOption
	inters     []Interceptor
	predicates []predicate.MDMCommand
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the MDMCommandQuery builder.
func (mcq *MDMCommandQuery) Where(ps ...predicate.MDMCommand) *MDMCommandQuery {
	mcq.predicates = append(mcq.predicates, ps...)
	return mcq
}

// Limit the number of records to be returned by this query.
func (mcq *MDMCommandQuery) Limit(limit int) *MDMCommandQuery {
	mcq.ctx.Limit = &limit
	return mcq
}

// Offset to start from.
func (mcq *MDMCommandQuery) Offset(offset int) *MDMCommandQuery {
	mcq.ctx.Offset = &offset
	return mcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (mcq *MDMCommandQuery) Unique(unique bool) *MDMCommandQuery {
	mcq.ctx.Unique = &unique
	return mcq
}

// Order specifies how the records should be ordered.
func (mcq *MDMCommandQuery) Order(o ...mdmcommand.OrderOption) *MDMCommandQuery {
	mcq.order = append(mcq.order, o...)
	return mcq
}

// First returns the first MDMCommand entity from the query.
// Returns a *NotFoundError when no MDMCommand was found.
func (mcq *MDMCommandQuery) First(ctx context.Context) (*MDMCommand, error) {
	nodes, err := mcq.Limit(1).All(setContextOp(ctx, mcq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{mdmcommand.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (mcq *MDMCommandQuery) FirstX(ctx context.Context) *MDMCommand {
	node, err := mcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first MDMCommand ID from the query.
// Returns a *NotFoundError when no MDMCommand ID was found.
func (mcq *MDMCommandQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = mcq.Limit(1).IDs(setContextOp(ctx, mcq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{mdmcommand.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (mcq *MDMCommandQuery) FirstIDX(ctx context.Context) string {
	id, err := mcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single MDMCommand entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one MDMCommand entity is found.
// Returns a *NotFoundError when no MDMCommand entities are found.
func (mcq *MDMCommandQuery) Only(ctx context.Context) (*MDMCommand, error) {
	nodes, err := mcq.Limit(2).All(setContextOp(ctx, mcq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{mdmcommand.Label}
	default:
		return nil, &NotSingularError{mdmcommand.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (mcq *MDMCommandQuery) OnlyX(ctx context.Context) *MDMCommand {
	node, err := mcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only MDMCommand ID in the query.
// Returns a *NotSingularError when more than one MDMCommand ID is found.
// Returns a *NotFoundError when no entities are found.
func (mcq *MDMCommandQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = mcq.Limit(2).IDs(setContextOp(ctx, mcq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{mdmcommand.Label}
	default:
		err = &NotSingularError{mdmcommand.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (mcq *MDMCommandQuery) OnlyIDX(ctx context.Context) string {
	id, err := mcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of MDMCommands.
func (mcq *MDMCommandQuery) All(ctx context.Context) ([]*MDMCommand, error) {
	ctx = setContextOp(ctx, mcq.ctx, ent.OpQueryAll)
	if err := mcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*MDMCommand, *MDMCommandQuery]()
	return withInterceptors[[]*MDMCommand](ctx, mcq, qr, mcq.inters)
}

// AllX is like All, but panics if an error occurs.
func (mcq *MDMCommandQuery) AllX(ctx context.Context) []*MDMCommand {
	nodes, err := mcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of MDMCommand IDs.
func (mcq *MDMCommandQuery) IDs(ctx context.Context) (ids []string, err error) {
	if mcq.ctx.Unique == nil && mcq.path != nil {
		mcq.Unique(true)
	}
	ctx = setContextOp(ctx, mcq.ctx, ent.OpQueryIDs)
	if err = mcq.Select(mdmcommand.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (mcq *MDMCommandQuery) IDsX(ctx context.Context) []string {
	ids, err := mcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (mcq *MDMCommandQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, mcq.ctx, ent.OpQueryCount)
	if err := mcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, mcq, querierCount[*MDMCommandQuery](), mcq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (mcq *MDMCommandQuery) CountX(ctx context.Context) int {
	count, err := mcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (mcq *MDMCommandQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, mcq.ctx, ent.OpQueryExist)
	switch _, err := mcq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (mcq *MDMCommandQuery) ExistX(ctx context.Context) bool {
	exist, err := mcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the MDMCommandQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (mcq *MDMCommandQuery) Clone() *MDMCommandQuery {
	if mcq == nil {
		return nil
	}
	return &MDMCommandQuery{
		config:     mcq.config,
		ctx:        mcq.ctx.Clone(),
		order:      append([]mdmcommand.OrderOption{}, mcq.order...),
		inters:     append([]Interceptor{}, mcq.inters...),
		predicates: append([]predicate.MDMCommand{}, mcq.predicates...),
		// clone intermediate query.
		sql:       mcq.sql.Clone(),
		path:      mcq.path,
		modifiers: append([]func(*sql.Selector){}, mcq.modifiers...),
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		When time.Time `json:"when,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.MDMCommand.Query().
//		GroupBy(mdmcommand.FieldWhen).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (mcq *MDMCommandQuery) GroupBy(field string, fields ...string) *MDMCommandGroupBy {
	mcq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &MDMCommandGroupBy{build: mcq}
	grbuild.flds = &mcq.ctx.Fields
	grbuild.label = mdmcommand.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		When time.Time `json:"when,omitempty"`
//	}
//
//	client.MDMCommand.Query().
//		Select(mdmcommand.FieldWhen).
//		Scan(ctx, &v)
func (mcq *MDMCommandQuery) Select(fields ...string) *MDMCommandSelect {
	mcq.ctx.Fields = append(mcq.ctx.Fields, fields...)
	sbuild := &MDMCommandSelect{MDMCommandQuery: mcq}
	sbuild.label = mdmcommand.Label
	sbuild.flds, sbuild.scan = &mcq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a MDMCommandSelect configured with the given aggregations.
func (mcq *MDMCommandQuery) Aggregate(fns ...AggregateFunc) *MDMCommandSelect {
	return mcq.Select().Aggregate(fns...)
}

func (mcq *MDMCommandQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range mcq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, mcq); err != nil {
				return err
			}
		}
	}
	for _, f := range mcq.ctx.Fields {
		if !mdmcommand.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if mcq.path != nil {
		prev, err := mcq.path(ctx)
		if err != nil {
			return err
		}
		mcq.sql = prev
	}
	return nil
}

func (mcq *MDMCommandQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*MDMCommand, error) {
	var (
		nodes = []*MDMCommand{}
		_spec = mcq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*MDMCommand).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &MDMCommand{config: mcq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(mcq.modifiers) > 0 {
		_spec.Modifiers = mcq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, mcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (mcq *MDMCommandQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := mcq.querySpec()
	if len(mcq.modifiers) > 0 {
		_spec.Modifiers = mcq.modifiers
	}
	_spec.Node.Columns = mcq.ctx.Fields
	if len(mcq.ctx.Fields) > 0 {
		_spec.Unique = mcq.ctx.Unique != nil && *mcq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, mcq.driver, _spec)
}

func (mcq *MDMCommandQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(mdmcommand.Table, mdmcommand.Columns, sqlgraph.NewFieldSpec(mdmcommand.FieldID, field.TypeString))
	_spec.From = mcq.sql
	if unique := mcq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if mcq.path != nil {
		_spec.Unique = true
	}
	if fields := mcq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, mdmcommand.FieldID)
		for i := range fields {
			if fields[i] != mdmcommand.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := mcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := mcq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := mcq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := mcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (mcq *MDMCommandQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(mcq.driver.Dialect())
	t1 := builder.Table(mdmcommand.Table)
	columns := mcq.ctx.Fields
	if len(columns) == 0 {
		columns = mdmcommand.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if mcq.sql != nil {
		selector = mcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if mcq.ctx.Unique != nil && *mcq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range mcq.modifiers {
		m(selector)
	}
	for _, p := range mcq.predicates {
		p(selector)
	}
	for _, p := range mcq.order {
		p(selector)
	}
	if offset := mcq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := mcq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (mcq *MDMCommandQuery) Modify(modifiers ...func(s *sql.Selector)) *MDMCommandSelect {
	mcq.modifiers = append(mcq.modifiers, modifiers...)
	return mcq.Select()
}

// MDMCommandGroupBy is the group-by builder for MDMCommand entities.
type MDMCommandGroupBy struct {
	selector
	build *MDMCommandQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (mcgb *MDMCommandGroupBy) Aggregate(fns ...AggregateFunc) *MDMCommandGroupBy {
	mcgb.fns = append(mcgb.fns, fns...)
	return mcgb
}

// Scan applies the selector query and scans the result into the given value.
func (mcgb *MDMCommandGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mcgb.build.ctx, ent.OpQueryGroupBy)
	if err := mcgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MDMCommandQuery, *MDMCommandGroupBy](ctx, mcgb.build, mcgb, mcgb.build.inters, v)
}

func (mcgb *MDMCommandGroupBy) sqlScan(ctx context.Context, root *MDMCommandQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(mcgb.fns))
	for _, fn := range mcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*mcgb.flds)+len(mcgb.fns))
		for _, f := range *mcgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*mcgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mcgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// MDMCommandSelect is the builder for selecting fields of MDMCommand entities.
type MDMCommandSelect struct {
	*MDMCommandQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (mcs *MDMCommandSelect) Aggregate(fns ...AggregateFunc) *MDMCommandSelect {
	mcs.fns = append(mcs.fns, fns...)
	return mcs
}

// Scan applies the selector query and scans the result into the given value.
func (mcs *MDMCommandSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mcs.ctx, ent.OpQuerySelect)
	if err := mcs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MDMCommandQuery, *MDMCommandSelect](ctx, mcs.MDMCommandQuery, mcs, mcs.inters, v)
}

func (mcs *MDMCommandSelect) sqlScan(ctx context.Context, root *MDMCommandQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(mcs.fns))
	for _, fn := range mcs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*mcs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (mcs *MDMCommandSelect) Modify(modifiers ...func(s *sql.Selector)) *MDMCommandSelect {
	mcs.modifiers = append(mcs.modifiers, modifiers...)
	return mcs
}
