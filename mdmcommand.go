// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/open-uem/ent/agent"
	"github.com/open-uem/ent/mdmcommand"
)

// MDMCommand is the model entity for the MDMCommand schema.
type MDMCommand struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// When holds the value of the "when" field.
	When time.Time `json:"when,omitempty"`
	// Type holds the value of the "type" field.
	Type mdmcommand.Type `json:"type,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the MDMCommandQuery when eager-loading is set.
	Edges             MDMCommandEdges `json:"edges"`
	agent_mdmcommands *string
	selectValues      sql.SelectValues
}

// MDMCommandEdges holds the relations/edges for other nodes in the graph.
type MDMCommandEdges struct {
	// Agents holds the value of the agents edge.
	Agents *Agent `json:"agents,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// AgentsOrErr returns the Agents value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e MDMCommandEdges) AgentsOrErr() (*Agent, error) {
	if e.Agents != nil {
		return e.Agents, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: agent.Label}
	}
	return nil, &NotLoadedError{edge: "agents"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*MDMCommand) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case mdmcommand.FieldID, mdmcommand.FieldType:
			values[i] = new(sql.NullString)
		case mdmcommand.FieldWhen:
			values[i] = new(sql.NullTime)
		case mdmcommand.ForeignKeys[0]: // agent_mdmcommands
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the MDMCommand fields.
func (mc *MDMCommand) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case mdmcommand.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				mc.ID = value.String
			}
		case mdmcommand.FieldWhen:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field when", values[i])
			} else if value.Valid {
				mc.When = value.Time
			}
		case mdmcommand.FieldType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field type", values[i])
			} else if value.Valid {
				mc.Type = mdmcommand.Type(value.String)
			}
		case mdmcommand.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field agent_mdmcommands", values[i])
			} else if value.Valid {
				mc.agent_mdmcommands = new(string)
				*mc.agent_mdmcommands = value.String
			}
		default:
			mc.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the MDMCommand.
// This includes values selected through modifiers, order, etc.
func (mc *MDMCommand) Value(name string) (ent.Value, error) {
	return mc.selectValues.Get(name)
}

// QueryAgents queries the "agents" edge of the MDMCommand entity.
func (mc *MDMCommand) QueryAgents() *AgentQuery {
	return NewMDMCommandClient(mc.config).QueryAgents(mc)
}

// Update returns a builder for updating this MDMCommand.
// Note that you need to call MDMCommand.Unwrap() before calling this method if this MDMCommand
// was returned from a transaction, and the transaction was committed or rolled back.
func (mc *MDMCommand) Update() *MDMCommandUpdateOne {
	return NewMDMCommandClient(mc.config).UpdateOne(mc)
}

// Unwrap unwraps the MDMCommand entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (mc *MDMCommand) Unwrap() *MDMCommand {
	_tx, ok := mc.config.driver.(*txDriver)
	if !ok {
		panic("ent: MDMCommand is not a transactional entity")
	}
	mc.config.driver = _tx.drv
	return mc
}

// String implements the fmt.Stringer.
func (mc *MDMCommand) String() string {
	var builder strings.Builder
	builder.WriteString("MDMCommand(")
	builder.WriteString(fmt.Sprintf("id=%v, ", mc.ID))
	builder.WriteString("when=")
	builder.WriteString(mc.When.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("type=")
	builder.WriteString(fmt.Sprintf("%v", mc.Type))
	builder.WriteByte(')')
	return builder.String()
}

// MDMCommands is a parsable slice of MDMCommand.
type MDMCommands []*MDMCommand
