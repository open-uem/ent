// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/open-uem/ent/nanomdmsettings"
	"github.com/open-uem/ent/predicate"
	"github.com/open-uem/ent/tenant"
)

// NanoMDMSettingsQuery is the builder for querying NanoMDMSettings entities.
type NanoMDMSettingsQuery struct {
	config
	ctx        *QueryContext
	order      []nanomdmsettings.OrderOption
	inters     []Interceptor
	predicates []predicate.NanoMDMSettings
	withTenant *TenantQuery
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the NanoMDMSettingsQuery builder.
func (nmsq *NanoMDMSettingsQuery) Where(ps ...predicate.NanoMDMSettings) *NanoMDMSettingsQuery {
	nmsq.predicates = append(nmsq.predicates, ps...)
	return nmsq
}

// Limit the number of records to be returned by this query.
func (nmsq *NanoMDMSettingsQuery) Limit(limit int) *NanoMDMSettingsQuery {
	nmsq.ctx.Limit = &limit
	return nmsq
}

// Offset to start from.
func (nmsq *NanoMDMSettingsQuery) Offset(offset int) *NanoMDMSettingsQuery {
	nmsq.ctx.Offset = &offset
	return nmsq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (nmsq *NanoMDMSettingsQuery) Unique(unique bool) *NanoMDMSettingsQuery {
	nmsq.ctx.Unique = &unique
	return nmsq
}

// Order specifies how the records should be ordered.
func (nmsq *NanoMDMSettingsQuery) Order(o ...nanomdmsettings.OrderOption) *NanoMDMSettingsQuery {
	nmsq.order = append(nmsq.order, o...)
	return nmsq
}

// QueryTenant chains the current query on the "tenant" edge.
func (nmsq *NanoMDMSettingsQuery) QueryTenant() *TenantQuery {
	query := (&TenantClient{config: nmsq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := nmsq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := nmsq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(nanomdmsettings.Table, nanomdmsettings.FieldID, selector),
			sqlgraph.To(tenant.Table, tenant.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, true, nanomdmsettings.TenantTable, nanomdmsettings.TenantColumn),
		)
		fromU = sqlgraph.SetNeighbors(nmsq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first NanoMDMSettings entity from the query.
// Returns a *NotFoundError when no NanoMDMSettings was found.
func (nmsq *NanoMDMSettingsQuery) First(ctx context.Context) (*NanoMDMSettings, error) {
	nodes, err := nmsq.Limit(1).All(setContextOp(ctx, nmsq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{nanomdmsettings.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (nmsq *NanoMDMSettingsQuery) FirstX(ctx context.Context) *NanoMDMSettings {
	node, err := nmsq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first NanoMDMSettings ID from the query.
// Returns a *NotFoundError when no NanoMDMSettings ID was found.
func (nmsq *NanoMDMSettingsQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = nmsq.Limit(1).IDs(setContextOp(ctx, nmsq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{nanomdmsettings.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (nmsq *NanoMDMSettingsQuery) FirstIDX(ctx context.Context) int {
	id, err := nmsq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single NanoMDMSettings entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one NanoMDMSettings entity is found.
// Returns a *NotFoundError when no NanoMDMSettings entities are found.
func (nmsq *NanoMDMSettingsQuery) Only(ctx context.Context) (*NanoMDMSettings, error) {
	nodes, err := nmsq.Limit(2).All(setContextOp(ctx, nmsq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{nanomdmsettings.Label}
	default:
		return nil, &NotSingularError{nanomdmsettings.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (nmsq *NanoMDMSettingsQuery) OnlyX(ctx context.Context) *NanoMDMSettings {
	node, err := nmsq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only NanoMDMSettings ID in the query.
// Returns a *NotSingularError when more than one NanoMDMSettings ID is found.
// Returns a *NotFoundError when no entities are found.
func (nmsq *NanoMDMSettingsQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = nmsq.Limit(2).IDs(setContextOp(ctx, nmsq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{nanomdmsettings.Label}
	default:
		err = &NotSingularError{nanomdmsettings.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (nmsq *NanoMDMSettingsQuery) OnlyIDX(ctx context.Context) int {
	id, err := nmsq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of NanoMDMSettingsSlice.
func (nmsq *NanoMDMSettingsQuery) All(ctx context.Context) ([]*NanoMDMSettings, error) {
	ctx = setContextOp(ctx, nmsq.ctx, ent.OpQueryAll)
	if err := nmsq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*NanoMDMSettings, *NanoMDMSettingsQuery]()
	return withInterceptors[[]*NanoMDMSettings](ctx, nmsq, qr, nmsq.inters)
}

// AllX is like All, but panics if an error occurs.
func (nmsq *NanoMDMSettingsQuery) AllX(ctx context.Context) []*NanoMDMSettings {
	nodes, err := nmsq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of NanoMDMSettings IDs.
func (nmsq *NanoMDMSettingsQuery) IDs(ctx context.Context) (ids []int, err error) {
	if nmsq.ctx.Unique == nil && nmsq.path != nil {
		nmsq.Unique(true)
	}
	ctx = setContextOp(ctx, nmsq.ctx, ent.OpQueryIDs)
	if err = nmsq.Select(nanomdmsettings.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (nmsq *NanoMDMSettingsQuery) IDsX(ctx context.Context) []int {
	ids, err := nmsq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (nmsq *NanoMDMSettingsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, nmsq.ctx, ent.OpQueryCount)
	if err := nmsq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, nmsq, querierCount[*NanoMDMSettingsQuery](), nmsq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (nmsq *NanoMDMSettingsQuery) CountX(ctx context.Context) int {
	count, err := nmsq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (nmsq *NanoMDMSettingsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, nmsq.ctx, ent.OpQueryExist)
	switch _, err := nmsq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (nmsq *NanoMDMSettingsQuery) ExistX(ctx context.Context) bool {
	exist, err := nmsq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the NanoMDMSettingsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (nmsq *NanoMDMSettingsQuery) Clone() *NanoMDMSettingsQuery {
	if nmsq == nil {
		return nil
	}
	return &NanoMDMSettingsQuery{
		config:     nmsq.config,
		ctx:        nmsq.ctx.Clone(),
		order:      append([]nanomdmsettings.OrderOption{}, nmsq.order...),
		inters:     append([]Interceptor{}, nmsq.inters...),
		predicates: append([]predicate.NanoMDMSettings{}, nmsq.predicates...),
		withTenant: nmsq.withTenant.Clone(),
		// clone intermediate query.
		sql:       nmsq.sql.Clone(),
		path:      nmsq.path,
		modifiers: append([]func(*sql.Selector){}, nmsq.modifiers...),
	}
}

// WithTenant tells the query-builder to eager-load the nodes that are connected to
// the "tenant" edge. The optional arguments are used to configure the query builder of the edge.
func (nmsq *NanoMDMSettingsQuery) WithTenant(opts ...func(*TenantQuery)) *NanoMDMSettingsQuery {
	query := (&TenantClient{config: nmsq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	nmsq.withTenant = query
	return nmsq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Username string `json:"username,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.NanoMDMSettings.Query().
//		GroupBy(nanomdmsettings.FieldUsername).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (nmsq *NanoMDMSettingsQuery) GroupBy(field string, fields ...string) *NanoMDMSettingsGroupBy {
	nmsq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &NanoMDMSettingsGroupBy{build: nmsq}
	grbuild.flds = &nmsq.ctx.Fields
	grbuild.label = nanomdmsettings.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Username string `json:"username,omitempty"`
//	}
//
//	client.NanoMDMSettings.Query().
//		Select(nanomdmsettings.FieldUsername).
//		Scan(ctx, &v)
func (nmsq *NanoMDMSettingsQuery) Select(fields ...string) *NanoMDMSettingsSelect {
	nmsq.ctx.Fields = append(nmsq.ctx.Fields, fields...)
	sbuild := &NanoMDMSettingsSelect{NanoMDMSettingsQuery: nmsq}
	sbuild.label = nanomdmsettings.Label
	sbuild.flds, sbuild.scan = &nmsq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a NanoMDMSettingsSelect configured with the given aggregations.
func (nmsq *NanoMDMSettingsQuery) Aggregate(fns ...AggregateFunc) *NanoMDMSettingsSelect {
	return nmsq.Select().Aggregate(fns...)
}

func (nmsq *NanoMDMSettingsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range nmsq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, nmsq); err != nil {
				return err
			}
		}
	}
	for _, f := range nmsq.ctx.Fields {
		if !nanomdmsettings.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if nmsq.path != nil {
		prev, err := nmsq.path(ctx)
		if err != nil {
			return err
		}
		nmsq.sql = prev
	}
	return nil
}

func (nmsq *NanoMDMSettingsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*NanoMDMSettings, error) {
	var (
		nodes       = []*NanoMDMSettings{}
		_spec       = nmsq.querySpec()
		loadedTypes = [1]bool{
			nmsq.withTenant != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*NanoMDMSettings).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &NanoMDMSettings{config: nmsq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(nmsq.modifiers) > 0 {
		_spec.Modifiers = nmsq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, nmsq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := nmsq.withTenant; query != nil {
		if err := nmsq.loadTenant(ctx, query, nodes,
			func(n *NanoMDMSettings) { n.Edges.Tenant = []*Tenant{} },
			func(n *NanoMDMSettings, e *Tenant) { n.Edges.Tenant = append(n.Edges.Tenant, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (nmsq *NanoMDMSettingsQuery) loadTenant(ctx context.Context, query *TenantQuery, nodes []*NanoMDMSettings, init func(*NanoMDMSettings), assign func(*NanoMDMSettings, *Tenant)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*NanoMDMSettings)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Tenant(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(nanomdmsettings.TenantColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.tenant_nanomdm
		if fk == nil {
			return fmt.Errorf(`foreign-key "tenant_nanomdm" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "tenant_nanomdm" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (nmsq *NanoMDMSettingsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := nmsq.querySpec()
	if len(nmsq.modifiers) > 0 {
		_spec.Modifiers = nmsq.modifiers
	}
	_spec.Node.Columns = nmsq.ctx.Fields
	if len(nmsq.ctx.Fields) > 0 {
		_spec.Unique = nmsq.ctx.Unique != nil && *nmsq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, nmsq.driver, _spec)
}

func (nmsq *NanoMDMSettingsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(nanomdmsettings.Table, nanomdmsettings.Columns, sqlgraph.NewFieldSpec(nanomdmsettings.FieldID, field.TypeInt))
	_spec.From = nmsq.sql
	if unique := nmsq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if nmsq.path != nil {
		_spec.Unique = true
	}
	if fields := nmsq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, nanomdmsettings.FieldID)
		for i := range fields {
			if fields[i] != nanomdmsettings.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := nmsq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := nmsq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := nmsq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := nmsq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (nmsq *NanoMDMSettingsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(nmsq.driver.Dialect())
	t1 := builder.Table(nanomdmsettings.Table)
	columns := nmsq.ctx.Fields
	if len(columns) == 0 {
		columns = nanomdmsettings.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if nmsq.sql != nil {
		selector = nmsq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if nmsq.ctx.Unique != nil && *nmsq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range nmsq.modifiers {
		m(selector)
	}
	for _, p := range nmsq.predicates {
		p(selector)
	}
	for _, p := range nmsq.order {
		p(selector)
	}
	if offset := nmsq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := nmsq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (nmsq *NanoMDMSettingsQuery) Modify(modifiers ...func(s *sql.Selector)) *NanoMDMSettingsSelect {
	nmsq.modifiers = append(nmsq.modifiers, modifiers...)
	return nmsq.Select()
}

// NanoMDMSettingsGroupBy is the group-by builder for NanoMDMSettings entities.
type NanoMDMSettingsGroupBy struct {
	selector
	build *NanoMDMSettingsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (nmsgb *NanoMDMSettingsGroupBy) Aggregate(fns ...AggregateFunc) *NanoMDMSettingsGroupBy {
	nmsgb.fns = append(nmsgb.fns, fns...)
	return nmsgb
}

// Scan applies the selector query and scans the result into the given value.
func (nmsgb *NanoMDMSettingsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, nmsgb.build.ctx, ent.OpQueryGroupBy)
	if err := nmsgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*NanoMDMSettingsQuery, *NanoMDMSettingsGroupBy](ctx, nmsgb.build, nmsgb, nmsgb.build.inters, v)
}

func (nmsgb *NanoMDMSettingsGroupBy) sqlScan(ctx context.Context, root *NanoMDMSettingsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(nmsgb.fns))
	for _, fn := range nmsgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*nmsgb.flds)+len(nmsgb.fns))
		for _, f := range *nmsgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*nmsgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := nmsgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// NanoMDMSettingsSelect is the builder for selecting fields of NanoMDMSettings entities.
type NanoMDMSettingsSelect struct {
	*NanoMDMSettingsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (nmss *NanoMDMSettingsSelect) Aggregate(fns ...AggregateFunc) *NanoMDMSettingsSelect {
	nmss.fns = append(nmss.fns, fns...)
	return nmss
}

// Scan applies the selector query and scans the result into the given value.
func (nmss *NanoMDMSettingsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, nmss.ctx, ent.OpQuerySelect)
	if err := nmss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*NanoMDMSettingsQuery, *NanoMDMSettingsSelect](ctx, nmss.NanoMDMSettingsQuery, nmss, nmss.inters, v)
}

func (nmss *NanoMDMSettingsSelect) sqlScan(ctx context.Context, root *NanoMDMSettingsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(nmss.fns))
	for _, fn := range nmss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*nmss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := nmss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (nmss *NanoMDMSettingsSelect) Modify(modifiers ...func(s *sql.Selector)) *NanoMDMSettingsSelect {
	nmss.modifiers = append(nmss.modifiers, modifiers...)
	return nmss
}
