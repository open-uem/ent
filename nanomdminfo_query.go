// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/open-uem/ent/agent"
	"github.com/open-uem/ent/nanomdminfo"
	"github.com/open-uem/ent/predicate"
)

// NanoMDMInfoQuery is the builder for querying NanoMDMInfo entities.
type NanoMDMInfoQuery struct {
	config
	ctx        *QueryContext
	order      []nanomdminfo.OrderOption
	inters     []Interceptor
	predicates []predicate.NanoMDMInfo
	withAgent  *AgentQuery
	withFKs    bool
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the NanoMDMInfoQuery builder.
func (nmiq *NanoMDMInfoQuery) Where(ps ...predicate.NanoMDMInfo) *NanoMDMInfoQuery {
	nmiq.predicates = append(nmiq.predicates, ps...)
	return nmiq
}

// Limit the number of records to be returned by this query.
func (nmiq *NanoMDMInfoQuery) Limit(limit int) *NanoMDMInfoQuery {
	nmiq.ctx.Limit = &limit
	return nmiq
}

// Offset to start from.
func (nmiq *NanoMDMInfoQuery) Offset(offset int) *NanoMDMInfoQuery {
	nmiq.ctx.Offset = &offset
	return nmiq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (nmiq *NanoMDMInfoQuery) Unique(unique bool) *NanoMDMInfoQuery {
	nmiq.ctx.Unique = &unique
	return nmiq
}

// Order specifies how the records should be ordered.
func (nmiq *NanoMDMInfoQuery) Order(o ...nanomdminfo.OrderOption) *NanoMDMInfoQuery {
	nmiq.order = append(nmiq.order, o...)
	return nmiq
}

// QueryAgent chains the current query on the "agent" edge.
func (nmiq *NanoMDMInfoQuery) QueryAgent() *AgentQuery {
	query := (&AgentClient{config: nmiq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := nmiq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := nmiq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(nanomdminfo.Table, nanomdminfo.FieldID, selector),
			sqlgraph.To(agent.Table, agent.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, nanomdminfo.AgentTable, nanomdminfo.AgentColumn),
		)
		fromU = sqlgraph.SetNeighbors(nmiq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first NanoMDMInfo entity from the query.
// Returns a *NotFoundError when no NanoMDMInfo was found.
func (nmiq *NanoMDMInfoQuery) First(ctx context.Context) (*NanoMDMInfo, error) {
	nodes, err := nmiq.Limit(1).All(setContextOp(ctx, nmiq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{nanomdminfo.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (nmiq *NanoMDMInfoQuery) FirstX(ctx context.Context) *NanoMDMInfo {
	node, err := nmiq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first NanoMDMInfo ID from the query.
// Returns a *NotFoundError when no NanoMDMInfo ID was found.
func (nmiq *NanoMDMInfoQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = nmiq.Limit(1).IDs(setContextOp(ctx, nmiq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{nanomdminfo.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (nmiq *NanoMDMInfoQuery) FirstIDX(ctx context.Context) int {
	id, err := nmiq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single NanoMDMInfo entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one NanoMDMInfo entity is found.
// Returns a *NotFoundError when no NanoMDMInfo entities are found.
func (nmiq *NanoMDMInfoQuery) Only(ctx context.Context) (*NanoMDMInfo, error) {
	nodes, err := nmiq.Limit(2).All(setContextOp(ctx, nmiq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{nanomdminfo.Label}
	default:
		return nil, &NotSingularError{nanomdminfo.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (nmiq *NanoMDMInfoQuery) OnlyX(ctx context.Context) *NanoMDMInfo {
	node, err := nmiq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only NanoMDMInfo ID in the query.
// Returns a *NotSingularError when more than one NanoMDMInfo ID is found.
// Returns a *NotFoundError when no entities are found.
func (nmiq *NanoMDMInfoQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = nmiq.Limit(2).IDs(setContextOp(ctx, nmiq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{nanomdminfo.Label}
	default:
		err = &NotSingularError{nanomdminfo.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (nmiq *NanoMDMInfoQuery) OnlyIDX(ctx context.Context) int {
	id, err := nmiq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of NanoMDMInfos.
func (nmiq *NanoMDMInfoQuery) All(ctx context.Context) ([]*NanoMDMInfo, error) {
	ctx = setContextOp(ctx, nmiq.ctx, ent.OpQueryAll)
	if err := nmiq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*NanoMDMInfo, *NanoMDMInfoQuery]()
	return withInterceptors[[]*NanoMDMInfo](ctx, nmiq, qr, nmiq.inters)
}

// AllX is like All, but panics if an error occurs.
func (nmiq *NanoMDMInfoQuery) AllX(ctx context.Context) []*NanoMDMInfo {
	nodes, err := nmiq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of NanoMDMInfo IDs.
func (nmiq *NanoMDMInfoQuery) IDs(ctx context.Context) (ids []int, err error) {
	if nmiq.ctx.Unique == nil && nmiq.path != nil {
		nmiq.Unique(true)
	}
	ctx = setContextOp(ctx, nmiq.ctx, ent.OpQueryIDs)
	if err = nmiq.Select(nanomdminfo.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (nmiq *NanoMDMInfoQuery) IDsX(ctx context.Context) []int {
	ids, err := nmiq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (nmiq *NanoMDMInfoQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, nmiq.ctx, ent.OpQueryCount)
	if err := nmiq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, nmiq, querierCount[*NanoMDMInfoQuery](), nmiq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (nmiq *NanoMDMInfoQuery) CountX(ctx context.Context) int {
	count, err := nmiq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (nmiq *NanoMDMInfoQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, nmiq.ctx, ent.OpQueryExist)
	switch _, err := nmiq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (nmiq *NanoMDMInfoQuery) ExistX(ctx context.Context) bool {
	exist, err := nmiq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the NanoMDMInfoQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (nmiq *NanoMDMInfoQuery) Clone() *NanoMDMInfoQuery {
	if nmiq == nil {
		return nil
	}
	return &NanoMDMInfoQuery{
		config:     nmiq.config,
		ctx:        nmiq.ctx.Clone(),
		order:      append([]nanomdminfo.OrderOption{}, nmiq.order...),
		inters:     append([]Interceptor{}, nmiq.inters...),
		predicates: append([]predicate.NanoMDMInfo{}, nmiq.predicates...),
		withAgent:  nmiq.withAgent.Clone(),
		// clone intermediate query.
		sql:       nmiq.sql.Clone(),
		path:      nmiq.path,
		modifiers: append([]func(*sql.Selector){}, nmiq.modifiers...),
	}
}

// WithAgent tells the query-builder to eager-load the nodes that are connected to
// the "agent" edge. The optional arguments are used to configure the query builder of the edge.
func (nmiq *NanoMDMInfoQuery) WithAgent(opts ...func(*AgentQuery)) *NanoMDMInfoQuery {
	query := (&AgentClient{config: nmiq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	nmiq.withAgent = query
	return nmiq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		AvailableDeviceCapacity float64 `json:"available_device_capacity,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.NanoMDMInfo.Query().
//		GroupBy(nanomdminfo.FieldAvailableDeviceCapacity).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (nmiq *NanoMDMInfoQuery) GroupBy(field string, fields ...string) *NanoMDMInfoGroupBy {
	nmiq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &NanoMDMInfoGroupBy{build: nmiq}
	grbuild.flds = &nmiq.ctx.Fields
	grbuild.label = nanomdminfo.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		AvailableDeviceCapacity float64 `json:"available_device_capacity,omitempty"`
//	}
//
//	client.NanoMDMInfo.Query().
//		Select(nanomdminfo.FieldAvailableDeviceCapacity).
//		Scan(ctx, &v)
func (nmiq *NanoMDMInfoQuery) Select(fields ...string) *NanoMDMInfoSelect {
	nmiq.ctx.Fields = append(nmiq.ctx.Fields, fields...)
	sbuild := &NanoMDMInfoSelect{NanoMDMInfoQuery: nmiq}
	sbuild.label = nanomdminfo.Label
	sbuild.flds, sbuild.scan = &nmiq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a NanoMDMInfoSelect configured with the given aggregations.
func (nmiq *NanoMDMInfoQuery) Aggregate(fns ...AggregateFunc) *NanoMDMInfoSelect {
	return nmiq.Select().Aggregate(fns...)
}

func (nmiq *NanoMDMInfoQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range nmiq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, nmiq); err != nil {
				return err
			}
		}
	}
	for _, f := range nmiq.ctx.Fields {
		if !nanomdminfo.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if nmiq.path != nil {
		prev, err := nmiq.path(ctx)
		if err != nil {
			return err
		}
		nmiq.sql = prev
	}
	return nil
}

func (nmiq *NanoMDMInfoQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*NanoMDMInfo, error) {
	var (
		nodes       = []*NanoMDMInfo{}
		withFKs     = nmiq.withFKs
		_spec       = nmiq.querySpec()
		loadedTypes = [1]bool{
			nmiq.withAgent != nil,
		}
	)
	if nmiq.withAgent != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, nanomdminfo.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*NanoMDMInfo).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &NanoMDMInfo{config: nmiq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(nmiq.modifiers) > 0 {
		_spec.Modifiers = nmiq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, nmiq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := nmiq.withAgent; query != nil {
		if err := nmiq.loadAgent(ctx, query, nodes, nil,
			func(n *NanoMDMInfo, e *Agent) { n.Edges.Agent = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (nmiq *NanoMDMInfoQuery) loadAgent(ctx context.Context, query *AgentQuery, nodes []*NanoMDMInfo, init func(*NanoMDMInfo), assign func(*NanoMDMInfo, *Agent)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*NanoMDMInfo)
	for i := range nodes {
		if nodes[i].agent_nanomdminfo == nil {
			continue
		}
		fk := *nodes[i].agent_nanomdminfo
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(agent.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "agent_nanomdminfo" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (nmiq *NanoMDMInfoQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := nmiq.querySpec()
	if len(nmiq.modifiers) > 0 {
		_spec.Modifiers = nmiq.modifiers
	}
	_spec.Node.Columns = nmiq.ctx.Fields
	if len(nmiq.ctx.Fields) > 0 {
		_spec.Unique = nmiq.ctx.Unique != nil && *nmiq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, nmiq.driver, _spec)
}

func (nmiq *NanoMDMInfoQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(nanomdminfo.Table, nanomdminfo.Columns, sqlgraph.NewFieldSpec(nanomdminfo.FieldID, field.TypeInt))
	_spec.From = nmiq.sql
	if unique := nmiq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if nmiq.path != nil {
		_spec.Unique = true
	}
	if fields := nmiq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, nanomdminfo.FieldID)
		for i := range fields {
			if fields[i] != nanomdminfo.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := nmiq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := nmiq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := nmiq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := nmiq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (nmiq *NanoMDMInfoQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(nmiq.driver.Dialect())
	t1 := builder.Table(nanomdminfo.Table)
	columns := nmiq.ctx.Fields
	if len(columns) == 0 {
		columns = nanomdminfo.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if nmiq.sql != nil {
		selector = nmiq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if nmiq.ctx.Unique != nil && *nmiq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range nmiq.modifiers {
		m(selector)
	}
	for _, p := range nmiq.predicates {
		p(selector)
	}
	for _, p := range nmiq.order {
		p(selector)
	}
	if offset := nmiq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := nmiq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (nmiq *NanoMDMInfoQuery) Modify(modifiers ...func(s *sql.Selector)) *NanoMDMInfoSelect {
	nmiq.modifiers = append(nmiq.modifiers, modifiers...)
	return nmiq.Select()
}

// NanoMDMInfoGroupBy is the group-by builder for NanoMDMInfo entities.
type NanoMDMInfoGroupBy struct {
	selector
	build *NanoMDMInfoQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (nmigb *NanoMDMInfoGroupBy) Aggregate(fns ...AggregateFunc) *NanoMDMInfoGroupBy {
	nmigb.fns = append(nmigb.fns, fns...)
	return nmigb
}

// Scan applies the selector query and scans the result into the given value.
func (nmigb *NanoMDMInfoGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, nmigb.build.ctx, ent.OpQueryGroupBy)
	if err := nmigb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*NanoMDMInfoQuery, *NanoMDMInfoGroupBy](ctx, nmigb.build, nmigb, nmigb.build.inters, v)
}

func (nmigb *NanoMDMInfoGroupBy) sqlScan(ctx context.Context, root *NanoMDMInfoQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(nmigb.fns))
	for _, fn := range nmigb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*nmigb.flds)+len(nmigb.fns))
		for _, f := range *nmigb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*nmigb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := nmigb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// NanoMDMInfoSelect is the builder for selecting fields of NanoMDMInfo entities.
type NanoMDMInfoSelect struct {
	*NanoMDMInfoQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (nmis *NanoMDMInfoSelect) Aggregate(fns ...AggregateFunc) *NanoMDMInfoSelect {
	nmis.fns = append(nmis.fns, fns...)
	return nmis
}

// Scan applies the selector query and scans the result into the given value.
func (nmis *NanoMDMInfoSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, nmis.ctx, ent.OpQuerySelect)
	if err := nmis.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*NanoMDMInfoQuery, *NanoMDMInfoSelect](ctx, nmis.NanoMDMInfoQuery, nmis, nmis.inters, v)
}

func (nmis *NanoMDMInfoSelect) sqlScan(ctx context.Context, root *NanoMDMInfoQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(nmis.fns))
	for _, fn := range nmis.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*nmis.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := nmis.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (nmis *NanoMDMInfoSelect) Modify(modifiers ...func(s *sql.Selector)) *NanoMDMInfoSelect {
	nmis.modifiers = append(nmis.modifiers, modifiers...)
	return nmis
}
