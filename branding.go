// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/open-uem/ent/branding"
)

// Branding is the model entity for the Branding schema.
type Branding struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Logo as base64 data URL
	LogoLight string `json:"logo_light,omitempty"`
	// Favicon as base64 data URL
	LogoSmall string `json:"logo_small,omitempty"`
	// Primary brand color (hex)
	PrimaryColor string `json:"primary_color,omitempty"`
	// Custom product name to display
	ProductName string `json:"product_name,omitempty"`
	// Login page background image as base64 data URL
	LoginBackgroundImage string `json:"login_background_image,omitempty"`
	// Welcome text shown on login page
	LoginWelcomeText string `json:"login_welcome_text,omitempty"`
	// Whether to display the version number in the header
	ShowVersion bool `json:"show_version,omitempty"`
	// URL or email for bug reports
	BugReportLink string `json:"bug_report_link,omitempty"`
	// URL or email for help/documentation
	HelpLink     string `json:"help_link,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Branding) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case branding.FieldShowVersion:
			values[i] = new(sql.NullBool)
		case branding.FieldID:
			values[i] = new(sql.NullInt64)
		case branding.FieldLogoLight, branding.FieldLogoSmall, branding.FieldPrimaryColor, branding.FieldProductName, branding.FieldLoginBackgroundImage, branding.FieldLoginWelcomeText, branding.FieldBugReportLink, branding.FieldHelpLink:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Branding fields.
func (b *Branding) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case branding.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			b.ID = int(value.Int64)
		case branding.FieldLogoLight:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field logo_light", values[i])
			} else if value.Valid {
				b.LogoLight = value.String
			}
		case branding.FieldLogoSmall:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field logo_small", values[i])
			} else if value.Valid {
				b.LogoSmall = value.String
			}
		case branding.FieldPrimaryColor:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field primary_color", values[i])
			} else if value.Valid {
				b.PrimaryColor = value.String
			}
		case branding.FieldProductName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field product_name", values[i])
			} else if value.Valid {
				b.ProductName = value.String
			}
		case branding.FieldLoginBackgroundImage:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field login_background_image", values[i])
			} else if value.Valid {
				b.LoginBackgroundImage = value.String
			}
		case branding.FieldLoginWelcomeText:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field login_welcome_text", values[i])
			} else if value.Valid {
				b.LoginWelcomeText = value.String
			}
		case branding.FieldShowVersion:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field show_version", values[i])
			} else if value.Valid {
				b.ShowVersion = value.Bool
			}
		case branding.FieldBugReportLink:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field bug_report_link", values[i])
			} else if value.Valid {
				b.BugReportLink = value.String
			}
		case branding.FieldHelpLink:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field help_link", values[i])
			} else if value.Valid {
				b.HelpLink = value.String
			}
		default:
			b.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Branding.
// This includes values selected through modifiers, order, etc.
func (b *Branding) Value(name string) (ent.Value, error) {
	return b.selectValues.Get(name)
}

// Update returns a builder for updating this Branding.
// Note that you need to call Branding.Unwrap() before calling this method if this Branding
// was returned from a transaction, and the transaction was committed or rolled back.
func (b *Branding) Update() *BrandingUpdateOne {
	return NewBrandingClient(b.config).UpdateOne(b)
}

// Unwrap unwraps the Branding entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (b *Branding) Unwrap() *Branding {
	_tx, ok := b.config.driver.(*txDriver)
	if !ok {
		panic("ent: Branding is not a transactional entity")
	}
	b.config.driver = _tx.drv
	return b
}

// String implements the fmt.Stringer.
func (b *Branding) String() string {
	var builder strings.Builder
	builder.WriteString("Branding(")
	builder.WriteString(fmt.Sprintf("id=%v, ", b.ID))
	builder.WriteString("logo_light=")
	builder.WriteString(b.LogoLight)
	builder.WriteString(", ")
	builder.WriteString("logo_small=")
	builder.WriteString(b.LogoSmall)
	builder.WriteString(", ")
	builder.WriteString("primary_color=")
	builder.WriteString(b.PrimaryColor)
	builder.WriteString(", ")
	builder.WriteString("product_name=")
	builder.WriteString(b.ProductName)
	builder.WriteString(", ")
	builder.WriteString("login_background_image=")
	builder.WriteString(b.LoginBackgroundImage)
	builder.WriteString(", ")
	builder.WriteString("login_welcome_text=")
	builder.WriteString(b.LoginWelcomeText)
	builder.WriteString(", ")
	builder.WriteString("show_version=")
	builder.WriteString(fmt.Sprintf("%v", b.ShowVersion))
	builder.WriteString(", ")
	builder.WriteString("bug_report_link=")
	builder.WriteString(b.BugReportLink)
	builder.WriteString(", ")
	builder.WriteString("help_link=")
	builder.WriteString(b.HelpLink)
	builder.WriteByte(')')
	return builder.String()
}

// Brandings is a parsable slice of Branding.
type Brandings []*Branding
