// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/open-uem/ent/nanomdmsettings"
)

// NanoMDMSettings is the model entity for the NanoMDMSettings schema.
type NanoMDMSettings struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Username holds the value of the "username" field.
	Username string `json:"username,omitempty"`
	// Password holds the value of the "password" field.
	Password string `json:"password,omitempty"`
	// ServerURL holds the value of the "server_url" field.
	ServerURL string `json:"server_url,omitempty"`
	// CaCerFile holds the value of the "ca_cer_file" field.
	CaCerFile string `json:"ca_cer_file,omitempty"`
	// ProfilePayloadID holds the value of the "profile_payload_id" field.
	ProfilePayloadID string `json:"profile_payload_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the NanoMDMSettingsQuery when eager-loading is set.
	Edges        NanoMDMSettingsEdges `json:"edges"`
	selectValues sql.SelectValues
}

// NanoMDMSettingsEdges holds the relations/edges for other nodes in the graph.
type NanoMDMSettingsEdges struct {
	// Tenant holds the value of the tenant edge.
	Tenant []*Tenant `json:"tenant,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// TenantOrErr returns the Tenant value or an error if the edge
// was not loaded in eager-loading.
func (e NanoMDMSettingsEdges) TenantOrErr() ([]*Tenant, error) {
	if e.loadedTypes[0] {
		return e.Tenant, nil
	}
	return nil, &NotLoadedError{edge: "tenant"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*NanoMDMSettings) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case nanomdmsettings.FieldID:
			values[i] = new(sql.NullInt64)
		case nanomdmsettings.FieldUsername, nanomdmsettings.FieldPassword, nanomdmsettings.FieldServerURL, nanomdmsettings.FieldCaCerFile, nanomdmsettings.FieldProfilePayloadID:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the NanoMDMSettings fields.
func (nms *NanoMDMSettings) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case nanomdmsettings.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			nms.ID = int(value.Int64)
		case nanomdmsettings.FieldUsername:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field username", values[i])
			} else if value.Valid {
				nms.Username = value.String
			}
		case nanomdmsettings.FieldPassword:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field password", values[i])
			} else if value.Valid {
				nms.Password = value.String
			}
		case nanomdmsettings.FieldServerURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field server_url", values[i])
			} else if value.Valid {
				nms.ServerURL = value.String
			}
		case nanomdmsettings.FieldCaCerFile:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field ca_cer_file", values[i])
			} else if value.Valid {
				nms.CaCerFile = value.String
			}
		case nanomdmsettings.FieldProfilePayloadID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field profile_payload_id", values[i])
			} else if value.Valid {
				nms.ProfilePayloadID = value.String
			}
		default:
			nms.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the NanoMDMSettings.
// This includes values selected through modifiers, order, etc.
func (nms *NanoMDMSettings) Value(name string) (ent.Value, error) {
	return nms.selectValues.Get(name)
}

// QueryTenant queries the "tenant" edge of the NanoMDMSettings entity.
func (nms *NanoMDMSettings) QueryTenant() *TenantQuery {
	return NewNanoMDMSettingsClient(nms.config).QueryTenant(nms)
}

// Update returns a builder for updating this NanoMDMSettings.
// Note that you need to call NanoMDMSettings.Unwrap() before calling this method if this NanoMDMSettings
// was returned from a transaction, and the transaction was committed or rolled back.
func (nms *NanoMDMSettings) Update() *NanoMDMSettingsUpdateOne {
	return NewNanoMDMSettingsClient(nms.config).UpdateOne(nms)
}

// Unwrap unwraps the NanoMDMSettings entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (nms *NanoMDMSettings) Unwrap() *NanoMDMSettings {
	_tx, ok := nms.config.driver.(*txDriver)
	if !ok {
		panic("ent: NanoMDMSettings is not a transactional entity")
	}
	nms.config.driver = _tx.drv
	return nms
}

// String implements the fmt.Stringer.
func (nms *NanoMDMSettings) String() string {
	var builder strings.Builder
	builder.WriteString("NanoMDMSettings(")
	builder.WriteString(fmt.Sprintf("id=%v, ", nms.ID))
	builder.WriteString("username=")
	builder.WriteString(nms.Username)
	builder.WriteString(", ")
	builder.WriteString("password=")
	builder.WriteString(nms.Password)
	builder.WriteString(", ")
	builder.WriteString("server_url=")
	builder.WriteString(nms.ServerURL)
	builder.WriteString(", ")
	builder.WriteString("ca_cer_file=")
	builder.WriteString(nms.CaCerFile)
	builder.WriteString(", ")
	builder.WriteString("profile_payload_id=")
	builder.WriteString(nms.ProfilePayloadID)
	builder.WriteByte(')')
	return builder.String()
}

// NanoMDMSettingsSlice is a parsable slice of NanoMDMSettings.
type NanoMDMSettingsSlice []*NanoMDMSettings
