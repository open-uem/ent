// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/open-uem/ent/enrollmenttoken"
	"github.com/open-uem/ent/site"
	"github.com/open-uem/ent/tenant"
)

// EnrollmentToken is the model entity for the EnrollmentToken schema.
type EnrollmentToken struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// The enrollment token value (UUID)
	Token string `json:"token,omitempty"`
	// Human-readable description for this token
	Description string `json:"description,omitempty"`
	// Maximum number of times this token can be used (0 = unlimited)
	MaxUses int `json:"max_uses,omitempty"`
	// Number of times this token has been used
	CurrentUses int `json:"current_uses,omitempty"`
	// When this token expires (nil = never)
	ExpiresAt *time.Time `json:"expires_at,omitempty"`
	// Whether this token is currently active
	Active bool `json:"active,omitempty"`
	// Created holds the value of the "created" field.
	Created time.Time `json:"created,omitempty"`
	// Modified holds the value of the "modified" field.
	Modified time.Time `json:"modified,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the EnrollmentTokenQuery when eager-loading is set.
	Edges                    EnrollmentTokenEdges `json:"edges"`
	site_enrollment_tokens   *int
	tenant_enrollment_tokens *int
	selectValues             sql.SelectValues
}

// EnrollmentTokenEdges holds the relations/edges for other nodes in the graph.
type EnrollmentTokenEdges struct {
	// Tenant holds the value of the tenant edge.
	Tenant *Tenant `json:"tenant,omitempty"`
	// Site holds the value of the site edge.
	Site *Site `json:"site,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// TenantOrErr returns the Tenant value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e EnrollmentTokenEdges) TenantOrErr() (*Tenant, error) {
	if e.Tenant != nil {
		return e.Tenant, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: tenant.Label}
	}
	return nil, &NotLoadedError{edge: "tenant"}
}

// SiteOrErr returns the Site value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e EnrollmentTokenEdges) SiteOrErr() (*Site, error) {
	if e.Site != nil {
		return e.Site, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: site.Label}
	}
	return nil, &NotLoadedError{edge: "site"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*EnrollmentToken) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case enrollmenttoken.FieldActive:
			values[i] = new(sql.NullBool)
		case enrollmenttoken.FieldID, enrollmenttoken.FieldMaxUses, enrollmenttoken.FieldCurrentUses:
			values[i] = new(sql.NullInt64)
		case enrollmenttoken.FieldToken, enrollmenttoken.FieldDescription:
			values[i] = new(sql.NullString)
		case enrollmenttoken.FieldExpiresAt, enrollmenttoken.FieldCreated, enrollmenttoken.FieldModified:
			values[i] = new(sql.NullTime)
		case enrollmenttoken.ForeignKeys[0]: // site_enrollment_tokens
			values[i] = new(sql.NullInt64)
		case enrollmenttoken.ForeignKeys[1]: // tenant_enrollment_tokens
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the EnrollmentToken fields.
func (et *EnrollmentToken) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case enrollmenttoken.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			et.ID = int(value.Int64)
		case enrollmenttoken.FieldToken:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field token", values[i])
			} else if value.Valid {
				et.Token = value.String
			}
		case enrollmenttoken.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				et.Description = value.String
			}
		case enrollmenttoken.FieldMaxUses:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field max_uses", values[i])
			} else if value.Valid {
				et.MaxUses = int(value.Int64)
			}
		case enrollmenttoken.FieldCurrentUses:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field current_uses", values[i])
			} else if value.Valid {
				et.CurrentUses = int(value.Int64)
			}
		case enrollmenttoken.FieldExpiresAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field expires_at", values[i])
			} else if value.Valid {
				et.ExpiresAt = new(time.Time)
				*et.ExpiresAt = value.Time
			}
		case enrollmenttoken.FieldActive:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field active", values[i])
			} else if value.Valid {
				et.Active = value.Bool
			}
		case enrollmenttoken.FieldCreated:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created", values[i])
			} else if value.Valid {
				et.Created = value.Time
			}
		case enrollmenttoken.FieldModified:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field modified", values[i])
			} else if value.Valid {
				et.Modified = value.Time
			}
		case enrollmenttoken.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field site_enrollment_tokens", value)
			} else if value.Valid {
				et.site_enrollment_tokens = new(int)
				*et.site_enrollment_tokens = int(value.Int64)
			}
		case enrollmenttoken.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field tenant_enrollment_tokens", value)
			} else if value.Valid {
				et.tenant_enrollment_tokens = new(int)
				*et.tenant_enrollment_tokens = int(value.Int64)
			}
		default:
			et.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the EnrollmentToken.
// This includes values selected through modifiers, order, etc.
func (et *EnrollmentToken) Value(name string) (ent.Value, error) {
	return et.selectValues.Get(name)
}

// QueryTenant queries the "tenant" edge of the EnrollmentToken entity.
func (et *EnrollmentToken) QueryTenant() *TenantQuery {
	return NewEnrollmentTokenClient(et.config).QueryTenant(et)
}

// QuerySite queries the "site" edge of the EnrollmentToken entity.
func (et *EnrollmentToken) QuerySite() *SiteQuery {
	return NewEnrollmentTokenClient(et.config).QuerySite(et)
}

// Update returns a builder for updating this EnrollmentToken.
// Note that you need to call EnrollmentToken.Unwrap() before calling this method if this EnrollmentToken
// was returned from a transaction, and the transaction was committed or rolled back.
func (et *EnrollmentToken) Update() *EnrollmentTokenUpdateOne {
	return NewEnrollmentTokenClient(et.config).UpdateOne(et)
}

// Unwrap unwraps the EnrollmentToken entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (et *EnrollmentToken) Unwrap() *EnrollmentToken {
	_tx, ok := et.config.driver.(*txDriver)
	if !ok {
		panic("ent: EnrollmentToken is not a transactional entity")
	}
	et.config.driver = _tx.drv
	return et
}

// String implements the fmt.Stringer.
func (et *EnrollmentToken) String() string {
	var builder strings.Builder
	builder.WriteString("EnrollmentToken(")
	builder.WriteString(fmt.Sprintf("id=%v, ", et.ID))
	builder.WriteString("token=")
	builder.WriteString(et.Token)
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(et.Description)
	builder.WriteString(", ")
	builder.WriteString("max_uses=")
	builder.WriteString(fmt.Sprintf("%v", et.MaxUses))
	builder.WriteString(", ")
	builder.WriteString("current_uses=")
	builder.WriteString(fmt.Sprintf("%v", et.CurrentUses))
	builder.WriteString(", ")
	if v := et.ExpiresAt; v != nil {
		builder.WriteString("expires_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("active=")
	builder.WriteString(fmt.Sprintf("%v", et.Active))
	builder.WriteString(", ")
	builder.WriteString("created=")
	builder.WriteString(et.Created.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("modified=")
	builder.WriteString(et.Modified.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// EnrollmentTokens is a parsable slice of EnrollmentToken.
type EnrollmentTokens []*EnrollmentToken
